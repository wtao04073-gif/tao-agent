---
phase: tech-design
status: draft
requirement_spec: docs/requirements.md
interface_audit: docs/pi-interface-audit.md
pi_version_pinned: "0.87.1"
---

# 技术方案：行业通用 AI 办公 Agent 平台

> 需求依据：[requirements.md](./requirements.md)（已定稿）
> 事实依据：[pi-interface-audit.md](./pi-interface-audit.md)（Pi v0.87.1 源码一手核实）
>
> **本方案的每个关键决策都可追溯到上述两份文件之一。凡与接口审计冲突的设计以审计为准。**

---

## 0. 三条决定全局的约束

在展开架构之前，先把接口审计中最硬的三个发现摆在前面——它们不是实现细节，而是架构的地基。

### 约束 A：Pi 无法在单进程内承载多租户

Pi 有 4 个进程级全局单例：模块导入时设置的默认 stream 函数、替换 `globalThis.fetch` 的 undici 全局 dispatcher、猴补 `process.stdout.write` 的 stdout 接管、按单一路径键的凭据与模型缓存。官方既无并发 session 契约，仓库内也无任何测试覆盖两个并发活跃 session。

**→ 隔离单元必须是操作系统进程。一会话一进程，不存在折中方案。**

### 约束 B：Pi 无任何执行隔离

仓库 README 原话：Pi 不含用于限制文件系统、进程、网络或凭据访问的内置权限系统，默认以启动它的用户与进程的权限运行。`bash` 工具**无默认超时**。官方对隔离的唯一回答是容器化整个进程。

**→ 沙箱是本产品必须自建的第一等工程，不是配置项。**

### 约束 C：Pi 的 session 存储不可替换，且零并发保护

`SessionManager` 硬依赖 `node:fs`、私有构造函数、以具体类而非接口暴露。零加锁：裸 `appendFileSync` 无栅栏、迁移时用截断模式打开文件、首条消息写入用 `wx` 标志会抛 EEXIST、全程无 fsync、读取时还会追加换行。`list()` 的复杂度是 O(所有 session 的所有字节)。

**→ 会话状态的权威副本必须在我们自己的数据库里；Pi 的 JSONL 降级为「单进程内的运行时工作副本」。**

---

## 1. 整体架构

### 1.1 分层

```
┌──────────────────────────────────────────────────────────────┐
│  入口层   桌面 Web │ 移动 Web │ (预留) App │ 管理后台         │
├──────────────────────────────────────────────────────────────┤
│  网关层   BFF / API Gateway                                   │
│           身份与租户识别 · 请求标准化 · 限流 · 审计           │
│           SSE/WebSocket 事件下发                              │
├──────────────────────────────────────────────────────────────┤
│  平台服务层（本产品的价值主体）                               │
│  ┌────────────┬────────────┬────────────┬─────────────────┐  │
│  │ 场景服务   │ 任务编排   │ 知识资产   │ 计量与配额      │  │
│  │ 场景卡定义 │ 状态机     │ 检索       │ 事件流          │  │
│  │ 表单校验   │ 检查点     │ 模板口径   │ 熔断            │  │
│  │ 指令编译   │ 子任务扇出 │ 意见回写   │ 用量看板        │  │
│  ├────────────┼────────────┼────────────┼─────────────────┤  │
│  │ 租户与权限 │ 技能管理   │ 产物服务   │ 模型路由        │  │
│  └────────────┴────────────┴────────────┴─────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  执行层   Runner 池（一会话一进程）                           │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ Agent Host（自研瘦宿主，内嵌 Pi SDK）                 │    │
│  │  ├ 权限门扩展（tool_call 默认拒绝 + 白名单）          │    │
│  │  ├ 自定义工具（表格计算/Office 产出/检索/子任务）     │    │
│  │  ├ 上下文扩展（知识注入 / 口径约束 / 意见回写）       │    │
│  │  ├ 计量扩展（事件流上报）                             │    │
│  │  └ Pi SDK：AgentSession（Agent Loop / steering / skill）│   │
│  └──────────────────────────────────────────────────────┘    │
│           沙箱边界：SaaS=microVM · 私有化=容器+seccomp        │
├──────────────────────────────────────────────────────────────┤
│  数据层   PostgreSQL（含 RLS） · Redis · 对象存储 · 向量检索  │
└──────────────────────────────────────────────────────────────┘
```

### 1.2 Pi 的嵌入方式：子进程 + 进程内 SDK（混合模式）

**决策**：每个活跃会话一个 Runner 子进程；子进程内**不用现成 CLI，而是自研瘦宿主内嵌 Pi SDK**。

| 方案 | 隔离 | 可编程注册工具 | 权限门 | 判定 |
|---|---|---|---|---|
| 进程内多 session（纯 SDK） | ❌ 4 个全局单例 | ✅ | ✅ | **排除**（约束 A） |
| 子进程跑现成 CLI（纯 RPC） | ✅ | ❌ RPC 不支持 | ⚠️ 仅磁盘扩展文件 | 仅作降级路径 |
| **子进程内嵌 SDK（混合）** | ✅ | ✅ | ✅ | **采纳** |

采纳理由：
1. 进程边界是 Pi 唯一真实提供的隔离边界，且可再嵌套容器/微虚机
2. RPC 面只有约 35 个操作且**无法编程注册自定义工具与权限门**，而这两项是 SaaS 硬需求；SDK 的 `AgentSession` 有约 90 个成员
3. 崩溃收敛：OOM、失控 bash、异常扩展只杀一个租户的子进程
4. Pi 官方的 subagent 示例本身就是子进程 fan-out，该模式经官方验证

**降级路径**：保留一条走现成 CLI + RPC 的实现（`RpcClient` 官方已提供），用于快速验证与故障兜底。两条路径共用同一 `RunnerAdapter` 接口。

### 1.3 Runner 生命周期

```
任务提交 → 编排器申请 Runner
   ├─ 温池命中（按 租户+镜像 键）→ 复用
   └─ 未命中 → 冷启动（拉起沙箱 + Agent Host）
          ↓
   注入：租户上下文 / 权限策略 / 技能清单 / 知识检索凭据 / 模型路由配置
          ↓
   执行（事件流实时上报网关与计量）
          ↓
   终态：完成 / 失败 / 超限 / 被取消
          ↓
   回收：镜像会话增量到数据库 → 销毁进程 → 清理临时卷
```

**关键参数**（一期默认值，可按租户覆盖）：

| 参数 | 默认 | 依据 |
|---|---|---|
| 单任务墙钟上限 | 30 分钟 | Pi 的 bash 无默认超时（审计 §6），必须外部兜底 |
| 单次 bash 超时 | 120 秒 | 同上 |
| Runner 内存上限 | 2 GB | 按 4 核 24 GB 环境，预留并发余量 |
| 单会话最大工具调用步数 | 200 步 | 防失控循环 |
| 温池空闲存活 | 10 分钟 | 平衡冷启动开销与资源占用 |
| 单租户并发 Runner | 按档位（免费 2 / 基础 5 / 专业 20） | 对齐 Spec 3.3 并发限制 |

---

## 2. 核心机制设计

### 2.1 异步任务与「执行中可继续对话」

这是 Spec 里用户最在意的能力。接口审计澄清了一个**必须修正的产品预期**：

> Pi 的 steering **不是工具执行中的打断**。消息落在当前 assistant turn 及其全部工具调用完成之后、下一次模型请求之前。**它永不取消执行中的工具，只有 `abort()` 能。**（审计 §5，对照 `agent-loop.ts:174-300` 验证）

因此产品的正确表达是「**已插入，将在当前步骤完成后送达**」，而非「立即打断」。原型与前端文案都按此口径。

**任务状态机**

```
     ┌──────────┐
     │ QUEUED   │ 排队中（等 Runner 或并发位）
     └────┬─────┘
          ↓
     ┌──────────┐   steer/followUp 入队 ┌─────────────┐
     │ RUNNING  │ ←────────────────────→│ 用户消息队列 │
     └────┬─────┘                       └─────────────┘
          ├──────────────→ ┌──────────────┐
          │                │ AWAIT_CONFIRM│ 等待确认（高危动作/信息缺口）
          │                └──────┬───────┘
          │                       ↓ 确认/拒绝
          ├───────────────────────┘
          ↓
   ┌──────┴───────┬────────────┬───────────┐
   ↓              ↓            ↓           ↓
SUCCEEDED     FAILED      CANCELLED    EXCEEDED
（可重试）    （保留中间物）            （超限，可续）
```

**事件桥接**：Pi 的事件（审计 §1 列出 11 个基础 + 14 个 session 事件）由计量扩展与宿主统一转成平台事件，经网关 SSE 推给前端。

> ⚠️ **两条不能搞错的语义**（审计 §1）：
> - `agent_end` **不等于**结束——重试、压缩、steering 都会越过它继续。**只有 `agent_settled` 表示 Pi 不会再自动继续**，任务终态判定必须用它。
> - `message_end.message` 是权威值，不要用 delta 自行拼接的结果落库。

**检查点与续跑**

Pi 自身不提供任务级检查点（它只有 session 树）。设计如下：

- 检查点单元 = **一个已完成的 turn**（`turn_end` 事件边界），此时 Pi 的 session 状态是自洽的
- 每个 `turn_end` 时，宿主把「session 增量 entry + 已产出中间物引用 + 当前步骤游标」写入数据库，构成检查点
- Runner 异常退出后，编排器按最后检查点重建：用 `SessionManager.inMemory(cwd, options, entries)` 从数据库**注水**已完成的 entry（审计 §4 确认该逃生口存在），跳过已完成步骤，从下一 turn 继续
- 由此满足验收项「执行节点强制重启后从检查点续跑，已完成步骤不重复消耗模型调用」

> ⚠️ 注水路径的 `persist = false`，不回写文件。故权威副本在数据库，Pi 的 JSONL 仅作运行时工作副本——这与约束 C 的结论一致。

### 2.2 子 Agent 并行

Pi 刻意不内置（审计 §3）。官方示例的真实做法是 spawn 子 `pi` 进程跑 `--mode json -p`，解析其 JSONL 并累加用量。

**采纳该模式，但收归平台管控**：

- 子任务不由扩展自行 spawn，而是**回调平台编排器申请子 Runner**——否则子进程绕过配额、审计与沙箱策略
- 主 Agent 通过自定义工具 `dispatch_subtask(goal, inputs, agent_profile)` 发起；工具实现向编排器下单，拿到子任务 id 后轮询/订阅结果
- 子 Runner 与主 Runner **同租户、同沙箱策略、独立会话上下文**，符合 Spec「子 Agent 间上下文隔离」
- 并发扇出度受租户档位与全局资源双重限制
- 失败回退：子任务失败时主 Agent 收到结构化错误，由其决定重试、降级或放弃

**代价与缓解**：每个子 Agent 一次完整进程启动。缓解手段为温池 + 只对「可并行且收益明确」的场景启用（多表核对、多份材料批量生成），不做无差别扇出。

### 2.3 权限门与沙箱

**权限门**（进程内，Pi 扩展实现）

用 `tool_call` 钩子做默认拒绝 + 白名单（审计 §6）。两条 fail-safe 特性对我们有利：
- `tool_call` handler **抛异常即阻断工具**
- 官方 `permission-gate.ts` 示例在 `!ctx.hasUI` 时默认阻断——正是无头 SaaS 的正确行为

策略来源：按 租户 × 场景 × 角色 下发的工具白名单。高危动作（对外发送、批量修改、删除）返回「需确认」，任务转 `AWAIT_CONFIRM`，前端弹确认卡片。

> ⚠️ `event.input` 原地修改后 **Pi 不再重新校验**（审计 §3）。因此改写参数的逻辑自身必须做完整校验，不能依赖 Pi 兜底。

**沙箱**（进程外，双形态必须分叉）

| 形态 | 实现 | 理由 |
|---|---|---|
| SaaS | microVM 级隔离 | 多租户高密度下威胁模型正确 |
| 私有化 | 容器 + seccomp 白名单 | **客户常拿不到 `/dev/kvm`**（客户自己跑在虚机里、嵌套虚拟化未开），microVM 方案直接装不上 |

两形态共用同一 `SandboxProvider` 抽象：`create / exec / pause / resume / kill` + 网络策略 + 资源上限。

**网络铁律（两形态一致）**：默认全断网，仅放通显式域名白名单，出网流量强制经审计代理。最常见的生产事故是隔离完美的沙箱配了无限制出网。

**工具级重定向接缝**：Pi 的每个内置工具都接受可插拔的 `*Operations`（`BashOperations`、`ReadOperations` 等），官方文档明确说是为了委派到远程系统。**这是把执行路由进按租户沙箱的干净接缝，无需 fork Pi。**

### 2.4 会话与状态持久化

**权威副本在 PostgreSQL，Pi 的 JSONL 是运行时工作副本。**

理由见约束 C。具体做法：

- 每个 Runner 用独立 `--session-dir`（租户隔离 + 避免文件争用）
- 宿主订阅 `entry_appended` 事件，把每条 entry 镜像写入数据库（保留 Pi 的 `id` / `parentId` 树结构，8 位十六进制 id）
- 会话列表、搜索、分页全部走数据库索引 —— **绝不调用 `SessionManager.list()`**（O(所有字节)，审计 §4）
- 恢复走 `inMemory()` 注水
- **分布式租约**：每个 session 同时只允许一个可写 Runner，用 Redis 租约 + 心跳实现。Pi 零加锁，两个 worker 碰同一文件会静默截断或抛 EEXIST，宿主必须自己保证独占

**明确不做**（一期）：不 fork `session-manager.ts`（约 2000 行），不押注实验性的格式 4 `Storage`/`SessionRepo`（其文档自称 shapes 可能无迁移地原地变更）。

### 2.5 知识资产与记忆回写（核心差异化）

三类资产对应三种注入方式：

| 资产 | 注入机制 | Pi 钩子 |
|---|---|---|
| **文档知识库** | 自定义检索工具，按需召回；结果带来源标识 | `pi.registerTool` |
| **模板与字段口径** | 任务启动时作为约束注入系统提示 | `before_agent_start` 改 `systemPromptOptions` |
| **修改意见** | 同类任务启动时作为「上次的反馈」前置注入 | `before_agent_start` 或 `pi.sendMessage(deliverAs:"nextTurn")` |

**修改意见回写链路**（这是护城河的具体实现）：

```
用户在产物上提修改意见
   ↓
意见 + 产物 diff + 任务场景 → 结构化抽取（分类：格式/口径/措辞/事实/结构）
   ↓
落库为约束条目（scope: 平台/租户/工作区/个人，含来源任务 id 与置信度）
   ↓
下次同场景任务启动时按 scope 优先级合并 → 注入系统提示
   ↓
验收方式：对比两次同类任务产出，确认上次被改的地方这次不再犯
```

**检索隔离**：向量检索必须强制携带租户过滤，物理隔离租户（namespace 或强制 payload 过滤，取决于选型），并为每个过滤字段建索引。分页后置鉴权，**绝不用大集合的 ListObjects 做预过滤**。

**权限读写分离**：知识库授权必须区分「可查看」与「可编辑/删除」——这是企业客户第一天就会提的需求（Dify 的已知缺陷正是授权即全权限）。

### 2.6 技能体系

Pi 的 skill 机制可直接复用，但有一个关键约束（审计 §7）：

> **没有专门的 skill 加载工具——触发方式就是对系统提示中所告知的绝对路径发起一次普通 `read` 调用。**

两个后果必须在设计中体现：
1. **不能禁用 `read`/`bash`**，否则模型自主调用 skill 失效
2. **skill 路径必须对工具所在沙箱可读** —— 沙箱挂载必须包含技能目录

**三级命名空间**（对齐 Spec 3.6）：平台共享 / 租户私有 / 会话临时，按优先级合并。

**动态注册**：用 `resources_discover` 钩子或 `skillsOverride` 在内存中构造 skill 对象。但**正文仍须落在某个路径上**，故实现为：数据库/对象存储是真源 → Runner 启动时按租户绑定关系物化到沙箱内临时目录 → 通过钩子声明发现。会话临时技能设生存期自动回收。

### 2.7 模型路由与成本

Pi 的 `pi-ai` 已提供多提供方统一接入与会话中途换模型（审计 §1），直接满足 Spec「模型可热替换」的硬要求。

**分层路由**：
- 默认轻量档执行
- 旗舰档仅用于任务规划与结果裁决节点（用 `scopedModels` 或运行时 `set_model`）
- 必须开启上下文缓存 —— 这是最大单点成本杠杆

**路由变更门禁**：覆盖两个行业核心场景的回归用例集，上线前必过。**静默质量退化不报错、不上仪表盘，两三天后以客服工单形式出现** —— 这条必须做成 CI 门禁而非人工约定。

**成本护栏**：
- 单用户级熔断（1% 的重度用户足以击穿席位制毛利）
- 计量埋点两形态完全一致；**结算层分叉**：SaaS 扣额度并熔断，私有化只上报不扣费、配额转为并发与速率硬限
- 额度向用户呈现的单位是**产物与任务**，不是会话次数（一个多步任务可能几十次工具调用但只交付一个 docx）

### 2.8 Office 产物与格式保真

直接对手的公开短板正是「导出到 WPS/Office 后格式跑偏」，这是可攻击的正面战场。

- 走**原生文件生成**路线（非 HTML 转换），产物真可编辑、可盖章流转
- 基于机构模板套版，保留公式与数据格式
- **生成后必须打开校验**：校验版式与可打开性，失败则自动重试或降级并告知用户
- 产物与源数据、任务、被引用知识片段保留关联，支持逐项追溯

---

## 3. 数据模型要点

**铁律：所有业务表无条件携带 `tenant_id`，并施加行级安全。私有化部署时租户数为 1，但模型不简化。**

这是双形态共用一套代码的前提，也是租户可在两形态间迁移的前提。**明确排除 schema-per-tenant**（云厂商官方定性为运维复杂且不更安全的双输区间）。

核心实体（字段略）：

| 域 | 实体 |
|---|---|
| 组织 | `tenants` / `workspaces` / `users` / `memberships`（角色） |
| 会话与任务 | `sessions` / `session_entries`（镜像 Pi 树，保留 id/parentId）/ `tasks` / `task_checkpoints` / `task_events` |
| 知识资产 | `knowledge_bases` / `documents` / `chunks` / `templates` / `field_definitions`（口径）/ `feedback_constraints`（意见回写）|
| 技能与场景 | `skills` / `tenant_skills` / `scenario_cards` / `scenario_versions` |
| 产物 | `artifacts` / `artifact_lineage`（溯源） |
| 计量 | `usage_events`（统一事件流）/ `quotas` / `billing_records` |
| 审计 | `audit_logs`（工具调用、权限拦截、资产变更、技能启停） |

**索引与隔离要求**：
- 所有查询路径的复合索引首列为 `tenant_id`
- 向量检索强制租户过滤 + 过滤字段建索引
- 会话列表走独立索引表，不做全量扫描

---

## 4. 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| Agent Host | **TypeScript / Node ≥ 22.19** | Pi SDK 是 TS，扩展由 jiti 转译无需构建；Node 版本是 Pi 的硬要求 |
| 平台服务 | TypeScript（NestJS 或 Fastify） | 与 Host 同语言，类型可共享，团队单一技术栈 |
| 前端 | React + TypeScript + Vite | 生态成熟；Pi 另提供 Lit 版 Web 组件库可作聊天区参考 |
| 数据库 | PostgreSQL（行级安全） | RLS 原生支持租户隔离；单库支撑双形态 |
| 缓存与队列 | Redis（缓存 + 租约 + 轻量队列） | 私有化单机部署可用，不引重组件 |
| 对象存储 | S3 兼容（私有化可用 MinIO） | 产物与技能包存储，双形态一致接口 |
| 向量检索 | 一期用 PostgreSQL 向量扩展 | **私有化优先**：少一个组件就少一份客户运维负担；规模上来再抽到独立服务 |
| 沙箱 | SaaS microVM / 私有化 容器+seccomp | 约束 B，物理限制导致必须分叉 |
| 部署 | 单机 Compose（私有化默认） | 客户不具备编排运维能力 |

**选型原则**：私有化形态的组件数量是交付成本的主要来源。**每多一个有状态组件，就多一份客户运维负担与故障面。** 因此一期刻意把向量检索放在 PostgreSQL 内，不引独立向量库。

---

## 5. 一期工程拆解

### 5.1 必须在一期完成的架构地基（后补是重写级代价）

1. **全表 `tenant_id` + 行级安全** —— 后补要改所有表与所有查询
2. **`SandboxProvider` 抽象** —— 后补会让两形态实现纠缠
3. **统一计量事件流** —— 后补无法回溯历史用量
4. **`RunnerAdapter` 抽象**（混合模式 / RPC 降级双实现） —— 后补会把 Pi 细节泄漏到平台层

### 5.2 里程碑

| 里程碑 | 交付内容 | 验证标准 |
|---|---|---|
| **M0 · 技术验证**（最高优先级） | 三个 spike：① 双并发 Runner 无串台 ② 检查点续跑不重复消耗 ③ 权限门默认拒绝生效 | 三项全部通过才进 M1 |
| **M1 · 最小闭环** | 单租户单场景打通：上传 Excel → Agent 核对 → 产出 xlsx。含 Agent Host、权限门、一个自定义工具、任务状态机、事件流 | 端到端跑通一个真实制造业对账场景 |
| **M2 · 多租户与场景化** | 租户三层模型、场景工作台、场景卡定义与表单校验、知识库检索、产物溯源 | 两行业各 5 个场景可用 |
| **M3 · 差异化能力** | 修改意见回写链路、模板口径资产、子 Agent 并行、技能三级命名空间 | 同类任务第二次产出不再犯上次被改的错 |
| **M4 · 商业化与交付** | 计量配额与熔断、管理后台与用量看板、移动 Web、单机 Compose 交付包与自检 | 客户 IT 人员 2 小时内独立装成 |

### 5.3 M0 的三个 spike（写代码前必须做）

接口审计列出的关键未知，必须用实验而非推理来消解：

**Spike 1 · 并发隔离验证**
目标：证伪或确认「进程内多 session 不可行」。做法：同进程起两个 `AgentSession`，用不同凭据与不同模型，并发跑，断言无串台、无 stdout 争用。
预期结果：失败（有 4 个全局单例）。**若失败即锁定一会话一进程架构；若意外成功，也不改架构——因为沙箱隔离需求独立成立。**

**Spike 2 · 检查点续跑**
目标：验证 `SessionManager.inMemory()` 注水路径能否支撑续跑。做法：跑一个多 turn 任务，在第 3 个 `turn_end` 后杀进程，从数据库注水重建，断言不重复调用模型、产出一致。
这是 Spec 验收项「强制重启后从检查点续跑」的唯一验证方式。

**Spike 3 · 权限门与沙箱**
目标：验证 `tool_call` 默认拒绝 + 白名单 + 抛异常阻断的实际行为，以及沙箱内 skill 路径可读性。做法：构造越权工具调用，断言被拦截并记审计；在断网沙箱内触发一次 skill 加载，断言 `read` 能打开技能路径。

---

## 6. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| Pi 上游变更破坏嵌入 | 高 | **锁定 0.87.1**；所有扩展只走官方扩展点、不改内核源码；`RunnerAdapter` 隔离 Pi 细节；升级前跑回归用例集 |
| 实验性格式 4 接口变动 | 中 | 一期不使用（其文档自称可能无迁移地原地变更） |
| 冷启动开销拖慢体验 | 中 | 温池 + 场景卡提交后立即返回任务 id、异步执行（产品形态本就是异步，天然容忍） |
| 私有化客户环境差异 | 高 | 安装前环境自检 + 容器方案不依赖 `/dev/kvm` + 自助排障知识库能回答「为什么出错」 |
| 路由优化导致静默质量退化 | 高 | 回归评测做成 CI 门禁，非人工约定 |
| 单租户重度用户击穿成本 | 中 | 单用户级熔断 + 按档位并发限制 |

---

## 7. 与需求的对应关系

| Spec 功能 | 本方案对应 | 关键依赖 |
|---|---|---|
| 3.1 场景工作台 | 场景服务（卡定义/表单校验/指令编译） | 平台自建 |
| 3.2 Agent 执行引擎 | Agent Host + Pi SDK；子 Agent 见 2.2 | Pi Agent Loop |
| 3.3 异步任务与并行会话 | 任务状态机 + 检查点（2.1） | Pi steering（语义已澄清） |
| 3.4 机构知识资产 | 2.5 三类资产三种注入 | `before_agent_start` / 自定义检索工具 |
| 3.5 交付物产出 | 2.8 原生文件 + 打开校验 | 自定义工具 |
| 3.6 技能体系 | 2.6 三级命名空间 + 动态注册 | `resources_discover` / `skillsOverride` |
| 3.7 工具与数据接入 | 自定义工具 + 网关转发 + 沙箱（2.3） | `registerTool` / `*Operations` |
| 3.8 账号租户权限 | 第 3 节数据模型 + 行级安全 | 平台自建 |
| 3.9 计量与配额 | 2.7 统一事件流 + 结算分叉 | 计量扩展 |
| 3.10 模型接入与路由 | 2.7 分层路由 + 缓存 + 门禁 | Pi `pi-ai` |
| 3.11 部署与交付 | 单机 Compose + 自检（第 4 节） | 平台自建 |

---

## 8. 待决事项

1. **沙箱具体技术选型** —— microVM 与容器方案的具体实现需按目标客户环境抽样调研后定，一期先落 `SandboxProvider` 抽象与容器实现
2. **向量检索的规模拐点** —— 一期用 PostgreSQL 向量扩展，需压测确定何时必须抽出独立服务
3. **Pi 上游跟随策略的具体节奏** —— 建议锁定 0.87.1，每季度评估一次升级，但需团队确认
4. **子 Agent 并行的收益验证** —— 每个子 Agent 一次完整进程启动，需在 M3 用真实场景验证收益是否覆盖开销
